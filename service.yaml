apiVersion: v1
kind: Service
metadata:
  name: guestbook
spec:
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30888  # ← 30000-32767 obligatoire !
  selector:
    app: guestbook
  type: NodePort
